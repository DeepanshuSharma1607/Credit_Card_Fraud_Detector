# Origin of the FastAPI backend, used ONLY by the Vite dev proxy (vite.config.js).
# The browser itself always calls the relative path /api/v1/prediction,
# so this never needs to be exposed to client-side code.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
